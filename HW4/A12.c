#include <stdio.h> //Объявить библиотеки ввода-вывода
int main(void)
{
int a, sum; //Объявить переменные

scanf ("%d", &a); //Считать целое число и записать его по адресу a
sum = a%10;
sum += (a/10)%10;
sum += (a/100)%10;
printf("%d", sum); //Вывести на экран ответ
return 0; //Завершить программу успешно
}
